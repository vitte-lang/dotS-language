space modules/package/backend/math_stats

proc empty_i64s() -> [i64]
{
    let out: [i64] = []
    give out
}

proc sort_i64(values: [i64]) -> [i64]
{
    give values
}

proc percentile_i64(values: [i64], p: i32) -> i64
{
    if values.len == 0 { give 0 }
    let s = sort_i64(values)
    let n: i32 = s.len
    let idx: i32 = ((n - 1) * p) / 100
    if idx < 0 { idx = 0 }
    if idx >= n { idx = n - 1 }
    give s[idx]
}
